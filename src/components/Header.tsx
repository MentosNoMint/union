const Header = () => {
    return (
        <header className={'w-full flex justify-between mt-7 px-[4.75rem]'}>
            <span>Logo</span>
            <div className={'flex gap-3 items-center'}>

            </div>
        </header>
    )
};

export default Header;